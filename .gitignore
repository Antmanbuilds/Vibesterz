# This is a showcase repository. It must never contain application source,
# build output, secrets or local tooling.

# Dependencies and build output
node_modules/
dist/
build/
.cache/

# Environment and secrets
.env
.env.*
*.pem
*.key

# Editor and OS files
.DS_Store
.idea/
.vscode/
*.log

# Local tooling and agent state (never publish)
.local/
.agents/
.replit
replit.nix
attached_assets/
